services:
  next:
    build:
      context: .
    ports:
      - 3000:3000
    environment:
      - APP_ENV=production
      - NODE_ENV=production
    restart: always
    command: ["pnpm", "run", "start"]
